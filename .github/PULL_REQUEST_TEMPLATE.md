## What this does

<!-- One or two lines. Link the issue it closes, if there is one. -->

## How it was tested

<!-- Live match, a specific screen, the dev build, whatever applies. -->

## Checklist

- [ ] `cargo check` and `tsc` are clean
- [ ] Builds with `npm run tauri build`
- [ ] Still read only: nothing reads game memory, injects, overlays, or sends data outside Riot's own endpoints
